version: "3.8"
services: 
    bot:
        container_name: crBot
        restart: always
        build: .
        env_file: 
            - ./.env
        volumes: 
            - ./:/src
        links:
            - mongo
    mongo:
        container_name: crmongo
        image: mongo
        env_file: 
            - ./.env
        volumes: 
            - ./data:/data/db
        ports: 
            - "27024:27024"